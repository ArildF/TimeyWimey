@using TimeyWimey.Model
@using TimeyWimey.Infrastructure
@inject TimeLineCalculator _lineCalculator;

@if (Day == null)
{
    return;
}

@foreach (var entry in Day.Entries)
{
    <TimeEntryDisplay Day="@Day" Entry="@entry"/>
}

@code {
    [Parameter]
    public Day? Day { get; set; }

}